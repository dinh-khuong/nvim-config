require("Comment").setup {
  padding = true,

  sticky = true,
  -- ignore = nil,
  -- extra = nil,
  toggler = {
    line = '<leader>/',
    block = '<leader>bc'
  },

  opleader = {
    line = '<leader>/',
    block = '<leader>bc'
  },

  mappings = {
    basic = true,
    extra = true,
  },

  -- pre_hook = nil,
  -- post_hook = nil,
}
